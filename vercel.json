{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/*.js": {
      "runtime": "@vercel/node@3.0.0"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/chatbot/(.*)", "destination": "/chatbot/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
